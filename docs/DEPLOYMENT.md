# Chatto Deployment Guide (Render Auto-Deploy)

Render deploys automatically on every commit to main.

## Workflow
feature branch → PR → merge → auto-deploy

## Troubleshooting
Check logs if build fails.
